import {Image} from 'react-native';

const images = {
  onBoardingImage: Image.resolveAssetSource(require('./onBoardingImage.png')),
  backgroundImage: Image.resolveAssetSource(require('./backgroundImage.png')),
};

export default images;
